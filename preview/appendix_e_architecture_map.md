# Appendix E: Architecture Map > 510,000 lines of TypeScript. Here's how to navigate it. --- ## The 2,200-File Source Tree Claude Code v2.1.87, as captured in the leaked npm source map, spans 510,000 lines across approximately 2,200 TypeScript and TSX files. This is a large codebase by any measure — comparable in size to a medium-large commercial application. The directory structure is organized by function rather than by type. You won't find a single `src/utils.ts` file containing everything miscellaneous; you'll find `src/utils/permissions/`, `src/utils/config.ts`, `src/utils/modelCost.ts`, each dedicated to a specific domain. This reflects a deliberate organizational philosophy: files that change together live together. ### High-Level Directory Map ``` src/ ├── buddy/ # Companion system (18 species, gacha, animation) ├── cli/ # CLI argument parsing and entry handlers │ └── handlers/ # Per-command CLI handlers (mcp, skills, etc.) ├── commands/ # All slash commands (each command is a directory) ├── components/ # React components for terminal UI (Ink-based) ├── hooks/ # React hooks for voice, session state, etc. ├── services/ # Core background services │ ├── bridge/ # Web-to-local relay │ ├── compact/ # Context compaction (4 layers) │ ├── mcp/ # Model Context Protocol server management │ ├── PromptSuggestion/ # Speculation system │ └── voiceStreamSTT.ts # Voice input (Deepgram WebSocket) ├── tools/ # Claude's tools (Bash, Edit, Read, Agent, etc.) │ ├── AgentTool/ # Subagent spawning and management │ ├── BashTool/ # Bash execution with permission checks │ └── ... # One directory per tool ├── types/ # TypeScript type definitions │ └── permissions.ts # The 8-layer permission type system └── utils/ # Shared utilities ├── config.ts # Settings loading and config management ├── hooks/ # Lifecycle hooks (hooksConfigManager.ts) ├── modelCost.ts # Pricing data for all models └── permissions/ # Permission decision tree (6 modes, 3 phases) ``` --- ## "Read This When..." — A Guided Navigation Each major file or directory serves a specific purpose. Here's when you'd want to read each one: ### `src/QueryEngine.ts` (Lines 209-639) **Read this when:** You want to understand how Claude Code decides what to do with your message, why sessions feel slow, or how the system prompt is assembled. This is the orchestrator — the file that coordinates everything happening during a session. It assembles the system prompt by combining default instructions, your CLAUDE.md, tool descriptions, and MCP tool declarations. It maintains conversation history and tracks token usage. It's the decision-making hub that everything else feeds into. The `fetchSystemPromptParts()` function is where your CLAUDE.md becomes part of what Claude sees. The token budget tracker in this file is what drives the "Context: X% remaining" display in the UI. **Key function:** `fetchSystemPromptParts()` — assembles the system prompt array from multiple sources. **Connected to:** `query.ts` (what it orchestrates), `src/utils/config.ts` (where it reads settings from), `src/services/extractMemories/` (where memories end up getting injected). --- ### `src/tools/BashTool/` (multiple files) **Read this when:** You want to understand why Claude sometimes refuses to run a specific command, how the permission matching works, or how to write effective permission rules. The BashTool is the most security-critical tool in Claude Code. Before any Bash command executes, it passes through: 1. **AST parsing** — tree-sitter-bash parses the command into an abstract syntax tree. If the parser doesn't understand a node type, it fails closed — the command requires manual permission regardless of allow rules. This is the "if I don't understand it, I don't trust it" principle in code. 2. **Environment variable stripping** — some variables (`NODE_ENV`, `LANG`, `TZ`) are stripped because they don't affect security. Others (`PATH`, `LD_PRELOAD`, `BASH_ENV`) are never stripped because modifying them could subvert security checks. 3. **Prefix matching** — the processed command is matched against allow/deny rules using the glob-like patterns in your settings.json. **Key files:** - `bashPermissions.ts` — the AST-based permission logic - `shellRuleMatching.ts` in `src/utils/permissions/` — the prefix matching algorithm **Connected to:** `src/utils/permissions/permissions.ts` (the main permission decision tree), settings.json's `permissions` section. --- ### `src/services/compact/` **Read this when:** You want to understand why sessions can run for hours without hitting the context limit, what `/compact` actually does internally, or why "Claude forgot something from earlier." This directory contains the four-layer context management system: - `compact.ts` (~60K) — Core compaction orchestration. The main `compactConversation()` function that handles `/compact` and auto-compact triggers. - `microCompact.ts` (~19K) — Cache-friendly cleanup of old tool results. This runs on every request, not just when you're approaching the limit. - `autoCompact.ts` (~12K) — Threshold calculation and proactive compaction. Contains the math for when to trigger. - `sessionMemoryCompact.ts` (~21K) — Lightweight structured memory compaction that preserves conversation structure. The four layers run in sequence on each request. Most of the time, only microCompact does anything (cleaning old tool results). When you approach the threshold, autoCompact takes over. **Connected to:** `src/QueryEngine.ts` (where compaction is triggered), `src/hooks/hooksConfigManager.ts` (CompactionStart/CompactionEnd hooks). --- ### `src/utils/permissions/permissions.ts` (Lines 473-1057) **Read this when:** You want to understand why a specific command was blocked, how to write deny rules, or why some operations always ask for permission no matter what. The main permission decision logic. Three phases: 1. **Phase 1 (absolute rules)** — deny rules, ask rules, and `tool.checkPermissions()`. The `.git/` and `.claude/` path protections live here. These cannot be overridden by any mode. 2. **Phase 2 (mode judgment)** — bypass mode? Always-allow rule? Otherwise, flag as "needs confirmation." 3. **Phase 3 (post-processing)** — `dontAsk` mode converts "needs confirmation" to "denied." `auto` mode (ANT-only) runs the AI classifier. Headless mode tries hooks then denies. **Key concept:** The denial tracker at `denialTracking.ts` (max 5 consecutive, max 10 total) prevents AI classifiers from locking agents in infinite denial loops. **Connected to:** `src/tools/BashTool/bashPermissions.ts` (Bash-specific permissions), `src/types/permissions.ts` (type definitions), settings.json's `permissions` section. --- ### `src/services/PromptSuggestion/speculation.ts` **Read this when:** You want to understand why file edits appear instantaneous after you click "Allow," or how to implement pre-execution patterns in your own systems. The speculation system creates an overlay filesystem in memory, pre-executes write operations while the permission dialog is displayed to the user, then either merges the overlay (if approved) or discards it (if denied). The user experiences zero delay after clicking Allow. Only Edit and Write tools can speculate. Bash and Agent tools cannot — the side effects of a Bash command (process spawning, network calls, etc.) can't be isolated in a filesystem overlay. **Connected to:** The permission system (speculation starts when a permission dialog would show), `src/tools/FileEditTool/`, `src/tools/FileWriteTool/`. --- ### `src/buddy/companion.ts` and `src/buddy/sprites.ts` **Read this when:** You're curious about the companion system, want to calculate your companion from your userId, or want to understand deterministic randomness patterns. `companion.ts` — the gacha algorithm. The `roll()` function (18 lines) is the core: hash(userId + SALT) → seed → mulberry32 PRNG → rarity → species → stats. The `getCompanion()` function demonstrates the anti-cheat "bones last" pattern. `sprites.ts` — all 18 ASCII sprite definitions, 3 frames each, plus the 8 hat definitions. The `{E}` placeholder is substituted with the companion's eye character at render time. **Connected to:** `src/buddy/CompanionSprite.tsx` (rendering), `src/buddy/useBuddyNotification.tsx` (the April 1 timing logic), `src/buddy/prompt.ts` (the system prompt addition that gives the companion a voice). --- ### `src/commands.ts` **Read this when:** You want to understand which commands are available, why some commands don't show up in `/help`, or what the difference between ANT and external users is. This file registers all slash commands and controls which ones are visible and executable. The `INTERNAL_ONLY_COMMANDS` constant (lines 225-254) lists all ANT-only commands. The `BRIDGE_SAFE` and `REMOTE_SAFE` arrays (lines 619-660) define which commands work in remote/mobile contexts. Commands have `isEnabled` and `isHidden` flags. `isEnabled: () => false` prevents execution. `isHidden: true` removes the command from `/help` output. The combination of both creates a stub: the command slot exists but nothing happens. **Connected to:** All `src/commands/*/` directories (each command's implementation), the feature flag system (`feature()` calls guard command registration). --- ### `src/utils/config.ts` **Read this when:** You want to understand how settings.json is loaded, what the priority order actually is, or how `${ENV_VAR}` interpolation works. This is the settings loader. It reads all five settings sources (policy, project, local, user, CLI), merges them in priority order, and exposes a `getConfig()` function that all other parts of the system use. The `${ENV_VAR}` interpolation happens here — when settings.json contains `"${GITHUB_TOKEN}"`, this file substitutes the actual environment variable value before the config is used. **Connected to:** Every file that calls `getConfig()` — which is most of the system. --- ### `src/cost-tracker.ts` and `src/utils/modelCost.ts` **Read this when:** You want to understand what `/cost` is actually measuring, or why your cost estimate doesn't match what you expected. `modelCost.ts` contains the pricing table: input price, output price, cache write price, and cache read price for each model. This is the source of truth for all cost calculations. `cost-tracker.ts` has a three-stage pipeline: 1. **Estimation** (before API call): rough estimate from character count, or precise estimate from the `countTokens` API endpoint 2. **Real tracking** (from API response): extracts `usage.input_tokens`, `usage.output_tokens`, `usage.cache_creation_input_tokens`, `usage.cache_read_input_tokens` 3. **Enforcement**: checks against `maxBudgetUsd` and blocks further queries if exceeded The `getAdvisorUsage()` function recursively collects costs from all child agents. When you run `/cost`, you see the sum of the parent session plus every subagent. **Connected to:** `src/QueryEngine.ts` (enforcement), Appendix F for the pricing table contents. --- ### `src/hooks/hooksConfigManager.ts` (Lines 28-100) **Read this when:** You want to understand how hooks are stored, how they fire, and why a hook sometimes doesn't work as expected. The hooks configuration manager loads hook definitions from settings.json and provides a `runHooks(eventName, inputData)` function. Each hook is run as a shell command with the input data piped as JSON to stdin. The exit code determines behavior. The timeout behavior is defined here: different hooks have different timeouts (SessionStart hooks get 30 seconds; UserPromptSubmit hooks get 10 seconds, since they need to be fast to avoid input lag). **Connected to:** `src/utils/hooks/` (the hook runner), settings.json's `hooks` section, Appendix A (the complete hooks reference). --- ## Core Execution Path When you press Enter in Claude Code, your input travels through this sequence: ``` User Input (keyboard) ↓ PromptInput.tsx # Captures input, validates, routes ↓ handlePromptSubmit.ts # Parses slash commands, images, file refs ↓ QueryEngine.ts # Orchestrates the full query lifecycle ↓ query.ts → queryLoop() # The main agent loop (tool calls + API) ↓ claude.ts → queryModel() # Anthropic API streaming call ↓ Tools (parallel/serial) # BashTool, Edit, Read, Agent, etc. ↓ Messages.tsx # Streams output back to terminal ``` ### Phase 1: PromptInput.tsx (Lines 984-1105) Before dispatching, PromptInput handles: - **Suggestion box dismissal** — if autocomplete is visible, Enter closes it - **Agent view detection** — if a subagent is running, input goes to that context - **Empty input guard** — empty submissions are swallowed - **Message queue check** — if previous query is still running, new input is enqueued ### Phase 2: handlePromptSubmit.ts (Lines 120-144) Classification and preprocessing. The `@filename` file reference syntax is expanded here — if you type `@src/utils/config.ts what does this do?`, the file content is injected before the query reaches the engine. ```typescript async function processUserInput(input: string) { if (input.startsWith('/')) return handleSlashCommand(input) if (hasImagePaste(input)) return handleImageInput(input) if (hasFileReference(input)) return expandFileReferences(input) return dispatchToQueryEngine(input) } ``` ### Phase 3: QueryEngine.ts (Lines 209-639) — The Orchestrator Assembles the system prompt array: ```typescript systemPrompt = [ customPrompt ?? defaultSystemPrompt, // CLAUDE.md or built-in default memoryMechanicsPrompt, // How to use the memory system appendSystemPrompt, // User-provided additions ] ``` Then `prependUserContext()` adds tool definitions, MCP tool descriptions, and skill declarations. ### Phase 4: query.ts → queryLoop() (Lines 219-1200+) — The Agent Loop The most complex file in the codebase. The loop where Claude Code's agency lives: ```typescript async function* queryLoop(messages, tools, options) { let response = await queryModel(messages, tools) while (response.stop_reason === 'tool_use') { const toolCalls = extractToolCalls(response) for (const toolCall of toolCalls) { yield* executeToolWithHooks(toolCall) // Pre/post hooks + permission } messages = [...messages, response, ...toolResults] response = await queryModel(messages, tools) // Next turn } yield finalResponse(response) } ``` **Five exit conditions:** 1. `stop_reason === 'end_turn'` — model finished 2. `stop_reason === 'tool_use'` with no tools available — impossible state 3. `max_turns` reached — configurable upper bound 4. Token budget exhausted — compaction

---

## ✋ You've reached the end of the free preview

This is **24%** of *Appendix E: Architecture Map*.

The full chapter is **8,451 words** — 6,451 more words covering:

- Phase 5: claude.ts → queryModel() — The API Call
- Phase 6: Tool Execution
- Phase 7: Messages.tsx — Terminal Rendering
- The Tool Layer
- BashTool (`src/tools/BashTool/`)
- FileEditTool, FileWriteTool, FileReadTool
- *...and 60 more sections*

### Get the Complete Book

**[Claude Unleashed: The Complete Guide to Building With Claude Code](https://phy041.gumroad.com/l/claude-unleashed)**

- 495,000+ words across 18 chapters + 7 appendices
- Source code citations from all 510,000 lines of leaked Claude Code
- Real stories, real numbers, real techniques
- Free updates forever

**$29.99** (or pay what you want, minimum $19.99)

→ **[Buy on Gumroad](https://phy041.gumroad.com/l/claude-unleashed)**

---

*Already bought it? Check your email for the download link.*
